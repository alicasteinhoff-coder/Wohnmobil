# Beispiel: Gmail SMTP (einfachste Option)
# Ersetze die Werte und speichere als .env (nicht ins Git)

NODE_ENV=development
PORT=3000

# SMTP Einstellungen
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=deine-email@gmail.com
MAIL_PASS=DEIN_APP_PASSWORT

# Absenderadresse
MAIL_FROM="CamperLife <noreply@deine-domain.de>"

# Hinweis: Für Gmail benötigst du ein App-Passwort (unter https://myaccount.google.com/apppasswords)
# Alternativ: nutze SendGrid/Mailgun/AWS SES und setze die entsprechenden Werte oben.
